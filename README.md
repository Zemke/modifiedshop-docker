Move `docker-compose.yml` and `modifiedshop-server.Dockerfile` into the root of your Modified Shop installation and run

```
docker-compose up
```

and it should then have started the application to be accessible under http://localhost:4000.
